import {describe, test} from './vitest';

describe('Project support', () => {
  test('read/update project description and name', async () => {});
  test('create/update project validation: name not empty, description not empty', async () => {});
});
