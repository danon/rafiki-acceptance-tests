import {describe, test} from './vitest';

describe('Project support', () => {
  test('read/update project description and name', () => {});
  test('create/update project validation: name not empty, description not empty', () => {});
});
