import {Dsl} from './dsl/Dsl';
import {describe, test} from './playwright';

describe('Project support', () => {
  test('read/update project description and name', async (dsl: Dsl) => {});
  test('create/update project validation: name not empty, description not empty', async (dsl: Dsl) => {});
});
