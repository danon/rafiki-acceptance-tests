import {describe, test} from './vitest';

describe('Invoicing', () => {
  test('project owner can generate a client invoice, containing project bills; assert invoice contains bills', () => {});
  test('project owner can download an invoice as a pdf; assert pdf is downloaded', () => {
    // approval tests
  });
  test('project contributor can generate a contributor invoice, containing his project bills; assert contains own bills', () => {});
  test('hourly bill is represented in invoice with an hour and rate; assert invoice contains hours and rates', () => {});
});
