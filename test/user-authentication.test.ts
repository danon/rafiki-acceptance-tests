import {describe} from './vitest';

describe('User authentication', () => {
  // allow a new user to sign up - that's accidental
  // should show loading state during state - that's ui
  // prevent registration with duplicate email
  // logout
  // login after registration (since registering already logs in? - Yea)
  // show error message for invalid password
  // show error message for too many attempts
});
