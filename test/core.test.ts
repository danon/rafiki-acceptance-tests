import {describe, test} from './vitest';

describe('Project bills and members', () => {
  test('project owner creates a project; assert project exists', () => {});
  test('project owner can add a bill; assert bill is added', () => {});
  test('project owner can invite a project member; assert user is a member', () => {});
  test('project contributor can add a bill; assert bill is added;', () => {});
  test('project owner can remove a project member from a project; assert user is not a member', () => {});
  test("user cannot access a project he's not a member of; assert cannot access", () => {});
});
